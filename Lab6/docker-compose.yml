services:

  mongo:
    image: mongo:8

    ports:
      - "27017:27017"

    volumes:
      - mongo_data:/data/db

  api:
    build: .

    ports:
      - "5000:5000"

    depends_on:
      - mongo

    environment:
      MONGO_URL: mongodb://mongo:27017

volumes:
  mongo_data: